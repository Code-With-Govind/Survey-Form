# Survey-Form
This is a simple survey form webpage built with HTML and CSS. It collects user information including name, email, age, current role, learning preferences, areas of interest (via checkboxes), and optional comments. The form features a responsive design with a clean, centered layout and basic styling for inputs, buttons, and form elements.
